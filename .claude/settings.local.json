{
  "permissions": {
    "allow": [
      "Read(//c/Users/zeust/.config/gh/**)",
      "Bash(curl -s -H \"Authorization: token $\\(git config --global github.token || cat ~/.config/gh/hosts.yml)",
      "Bash(python3 -c \"import sys,json; runs=json.load\\(sys.stdin\\).get\\('workflow_runs',[]\\); [print\\(r['name'], r['status'], r['conclusion'], r['html_url']\\) for r in runs[:3]]\")"
    ]
  }
}
